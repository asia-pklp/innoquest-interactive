version: '3.8'

services:
  web:
    container_name: innoquest-game
    image: innoquest-game
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
